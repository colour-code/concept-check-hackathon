HYPOTHESIS — Track A: Concept Check
Committed before first line of code.

Claim:
A 100x learner who can name system components (frontend, backend, 
database, server) will fail to derive WHY the two-server split 
(LLM server vs functional server) exists from first principles, 
when pushed with one sharp follow-up question.

Result that proves me right:
2 or more out of 3 people cannot derive the two-server split 
without being told the answer.

Result that means no problem exists:
All 3 derive it cleanly on their own, unprompted.

Kill-number:
Only 1 out of 3 fails. If just 1 person struggles, 
the gap is not real ; it is just that person.

Observed so far (Move 1 evidence):
- Person 1: missed server entirely, could not place compute
- Person 2: named two servers but could not derive why the split exists
- Person 3: used "brain" metaphor for LLM server, borrowed from 
  architecture analogy, could not derive from first principles

Hypothesis status: OPEN. To be tested with the live tool in Move 5.
